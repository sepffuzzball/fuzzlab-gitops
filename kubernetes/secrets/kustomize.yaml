apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: secrets
  namespace: flux-system
spec:
  interval: 10m0s
  sourceRef:
    kind: GitRepository
    name: fuzzlab-gitops
  path: ./
  prune: true
  decryption:
    provider: sops
    secretRef:
      name: sops-age