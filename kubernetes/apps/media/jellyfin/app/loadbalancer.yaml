apiVersion: v1
kind: Service
metadata:
  name: jellyfin
  namespace: media
spec:
  selector:
    app.kubernetes.io/name: jellyfin
  ports:
    - port: 80
      targetPort: 80
      name: web
      protocol: TCP
  type: LoadBalancer
  loadBalancerIP: 10.0.2.65
  externalTrafficPolicy: Cluster