---
apiVersion: helm.toolkit.fluxcd.io/v2beta2
kind: HelmRelease
metadata:
  name: &app readarr-audio
  namespace: media
spec:
  interval: 30m
  chart:
    spec:
      chart: app-template
      version: 2.4.0
      sourceRef:
        kind: HelmRepository
        name: bjw-s
        namespace: flux-system
  maxHistory: 2
  install:
    remediation:
      retries: 3
  upgrade:
    cleanupOnFail: true
    remediation:
      retries: 3
  uninstall:
    keepHistory: false
  dependsOn:
    - name: csi-driver-cephfs
      namespace: storage
  values:
    controllers:
      main:
        volumes:
        - name: tmp
          emptyDir:
            medium: Memory

        pod:
          securityContext:
            runAsUser: ${PUID}
            runAsGroup: ${PGID}
            runAsNonRoot: true
            fsGroup: ${PGID}
            fsGroupChangePolicy: OnRootMismatch

          initContainers:
            01-init-db:
             image:
               repository: ghcr.io/onedr0p/postgres-init
               tag: 16
             imagePullPolicy: IfNotPresent
             env:
               INIT_POSTGRES_DBNAME: ${APP} ${APP}-log
               INIT_POSTGRES_HOST:
                 valueFrom:
                   secretKeyRef:
                     name: ${DB_USER}-db-secret
                     key: INIT_POSTGRES_HOST
               INIT_POSTGRES_USER:
                 valueFrom:
                   secretKeyRef:
                     name: ${DB_USER}-db-secret
                     key: INIT_POSTGRES_USER
               INIT_POSTGRES_PASS:
                 valueFrom:
                   secretKeyRef:
                     name: ${DB_USER}-db-secret
                     key: INIT_POSTGRES_PASS
               INIT_POSTGRES_SUPER_PASS:
                valueFrom:
                   secretKeyRef:
                     name: ${DB_USER}-db-secret
                     key: INIT_POSTGRES_SUPER_PASS

        containers:
          main:
            volumeMounts:
            - name: tmp
              mountPath: /tmp
            image:
              repository: ghcr.io/joryirving/readarr
              tag: 0.4.12.2753@sha256:bac135a3360f34b30d8afecafdd104bbd5cef90dbb34dc265298272a83a087e1
            env:
              TZ: ${TIMEZONE}
              READARR__APP__INSTANCE_NAME: readarr-audio
              READARR__PORT: &port 80
              READARR__LOG__LEVEL: info
              READARR__AUTH__METHOD: External
              READARR__APP__THEME: dark
              READARR__POSTGRES__HOST:
                valueFrom:
                  secretKeyRef:
                    name: ${DB_USER}-db-secret
                    key: INIT_POSTGRES_HOST
              READARR__POSTGRES__MAINDB: "${DB_NAME}"
              READARR__POSTGRES__LOGDB: "${DB_NAME}-log"
              READARR__POSTGRES__CACHEDB: "${DB_NAME}-cache"
              READARR__POSTGRES__PASSWORD:
                valueFrom:
                  secretKeyRef:
                    name: ${DB_USER}-db-secret
                    key: INIT_POSTGRES_PASS
              READARR__POSTGRES__USER:
                valueFrom:
                  secretKeyRef:
                    name: ${DB_USER}-db-secret
                    key: INIT_POSTGRES_USER
              READARR__API_KEY:
                valueFrom:
                  secretKeyRef:
                    name: cluster-media-secrets
                    key: "READARR_0_API_KEY"
            resources:
              requests:
                cpu: 18m
                memory: 128Mi
              limits:
                memory: 512Mi
    service:
      main:
        ports:
          http:
            port: *port
    ingress:
      main:
        enabled: true
        className: internal
        hosts:
          - host: &host "readarr-audio.${SECRET_DOMAIN_LOCAL}"
            paths:
              - path: /
                service:
                  name: main
                  path: http
        #tls:
        #  - hosts:
        #      - *host
    persistence:
      config:
        enabled: true
        existingClaim: readarr-audio-config
      lcl:
        enabled: true
        existingClaim: lcl
      downloads:
        enabled: true
        existingClaim: lcl-downloads
        mountPath: /downloads
