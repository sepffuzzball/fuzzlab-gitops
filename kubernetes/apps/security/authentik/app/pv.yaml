---
apiVersion: v1
kind: PersistentVolume
metadata:
  name: authentik-db-pv
spec:
  storageClassName: csi-cephfs-sc
  claimRef:
    name: authentik-db
    namespace: security
  capacity:
    storage: 5Gi
  volumeMode: Filesystem
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  csi:
    driver: cephfs.csi.ceph.com
    nodeStageSecretRef:
      # node stage secret name
      name: csi-cephfs-secret
      # node stage secret namespace where above secret is created
      namespace: kube-system
    volumeAttributes:
      # Required options from storageclass parameters need to be added in volumeAttributes
      "clusterID": "872c236b-2dc0-4856-81b2-e04499487caa"
      "fsName": "k8s-cephfs"
      "staticVolume": "true"
      "rootPath": /volumes/csi/k8s-security/authentik/db
    # volumeHandle can be anything, need not to be same
    # as PV name or volume name. keeping same for brevity
    volumeHandle: authentikdb

---
apiVersion: v1
kind: PersistentVolume
metadata:
  name: authentik-dbbak-pv
spec:
  storageClassName: csi-cephfs-sc
  claimRef:
    name: authentik-dbbak
    namespace: security
  capacity:
    storage: 5Gi
  volumeMode: Filesystem
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  csi:
    driver: cephfs.csi.ceph.com
    nodeStageSecretRef:
      # node stage secret name
      name: csi-cephfs-secret
      # node stage secret namespace where above secret is created
      namespace: kube-system
    volumeAttributes:
      # Required options from storageclass parameters need to be added in volumeAttributes
      "clusterID": "872c236b-2dc0-4856-81b2-e04499487caa"
      "fsName": "k8s-cephfs"
      "staticVolume": "true"
      "rootPath": /volumes/csi/k8s-security/authentik/dbbak
    # volumeHandle can be anything, need not to be same
    # as PV name or volume name. keeping same for brevity
    volumeHandle: authentikdbbak

---
apiVersion: v1
kind: PersistentVolume
metadata:
  name: authentik-redis-pv
spec:
  storageClassName: csi-cephfs-sc
  claimRef:
    name: authentik-redis
    namespace: security
  capacity:
    storage: 1Gi
  volumeMode: Filesystem
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  csi:
    driver: cephfs.csi.ceph.com
    nodeStageSecretRef:
      # node stage secret name
      name: csi-cephfs-secret
      # node stage secret namespace where above secret is created
      namespace: kube-system
    volumeAttributes:
      # Required options from storageclass parameters need to be added in volumeAttributes
      "clusterID": "872c236b-2dc0-4856-81b2-e04499487caa"
      "fsName": "k8s-cephfs"
      "staticVolume": "true"
      "rootPath": /volumes/csi/k8s-security/authentik/redis
    # volumeHandle can be anything, need not to be same
    # as PV name or volume name. keeping same for brevity
    volumeHandle: authentikredis

---
apiVersion: v1
kind: PersistentVolume
metadata:
  name: authentik-media-pv
spec:
  storageClassName: csi-cephfs-sc
  claimRef:
    name: authentik-media
    namespace: security
  capacity:
    storage: 1Gi
  volumeMode: Filesystem
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  csi:
    driver: cephfs.csi.ceph.com
    nodeStageSecretRef:
      # node stage secret name
      name: csi-cephfs-secret
      # node stage secret namespace where above secret is created
      namespace: kube-system
    volumeAttributes:
      # Required options from storageclass parameters need to be added in volumeAttributes
      "clusterID": "872c236b-2dc0-4856-81b2-e04499487caa"
      "fsName": "k8s-cephfs"
      "staticVolume": "true"
      "rootPath": /volumes/csi/k8s-security/authentik/media
    # volumeHandle can be anything, need not to be same
    # as PV name or volume name. keeping same for brevity
    volumeHandle: authentikmedia


---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: authentik-db
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: csi-cephfs-sc
  volumeName: authentik-db-pv

---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: authentik-dbback
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: csi-cephfs-sc
  volumeName: authentik-dbback-pv

---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: authentik-redis
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: csi-cephfs-sc
  volumeName: authentik-redis-pv

---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: authentik-media
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: csi-cephfs-sc
  volumeName: authentik-media-pv