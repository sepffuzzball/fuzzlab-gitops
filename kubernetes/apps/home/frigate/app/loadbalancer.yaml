apiVersion: v1
kind: Service
metadata:
  name: frigate
  namespace: home
spec:
  selector:
    app.kubernetes.io/name: frigate
  ports:
    - port: 80
      targetPort: 80
      name: web
      protocol: TCP
  type: LoadBalancer
  loadBalancerIP: 10.0.2.66
  externalTrafficPolicy: Cluster